#include "main.h"

/**
 * _getline - gets LINE from environ
 * @name: pointer to PATH string
 *
 * Return: pointer to PATH member string or NULL if not found
 */
 istream& getline(istream& ins, MyString& target)
{

    char ch = 0;
    istream& get(char& ch);
    while (ins && ch != '\n')
    {
        istream& get(char& ch);
        target += ch;
    };
    return ins;
};
